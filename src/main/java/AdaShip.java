package main.java;

// Main class
public class AdaShip {
    // Run the main
    public static void main(String args[]) {
        Initialise initialise = new Initialise();
        initialise.setup();
        initialise.launch(); 
    }
}
